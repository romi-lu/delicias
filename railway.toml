# Cuando el servicio usa la raíz del repo (sin Root Directory), fuerza Docker.
[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
healthcheckPath = "/"
# Segundos; si Postgres tarda en aceptar conexiones en el primer arranque
healthcheckTimeout = 300
